# Python dependencies for the `phd-proficiency` env (pyenv-virtualenv, Python 3.14).
# Install:  python3 -m pip install -r requirements.txt
#
# Add packages here as helper scripts need them.
#
# Used by scripts/fetch_syllabi.py (fetch + parse YTU Bologna syllabus pages):
requests
beautifulsoup4
#
# Used by scripts/extract_pdf_text.py (per-page text extraction from lecture/exam PDFs):
pypdf
#
# Suggested starting set for control-systems + ML study (uncomment what you use):
#
# numpy
# scipy
# sympy
# matplotlib
# control          # python-control: state-space, transfer functions, LTI tools
# python-docx      # parse the .docx exam papers
# scikit-learn
# jupyter
